READ.ME

Problem:

Load integers from a file, sort them, and write the sorted integers to another file.

Instructions:

To compile the source code, simply enter "make", and the makefile will do the rest.
To run the executable, simply enter "./sort", and the executable will run.
